#==========================================================================
# Function: reap
#==========================================================================
function reap
{
   local pid=$1
   local time=$2

   sleep $time
   kill $pid
}
